#foreach($list in $sbList)
	#foreach($log in $list)
		<a target="_blank" href="projects/$projectKey?selectedItem=build-log:page&projResultId=$log.sbProjResultId&issueKey=$issueKey">
		<span class='square-box-$log.sbProjResult'/>
		<span class="tooltiptext">
			&nbsp;&nbsp;BuildId : $log.sbProjResultId
		</span></a>
	#end
	<br/>
#end
